The grammar I created mostly uses PTB tags, but there are some slight differences.

1. I create an NPP as for NP + PP.
2. I change PRP$ (possessive pronoun) to PRPS.
3. For the SBAR, I made SBAR -> IN CL. That is, instead of reuse S, I create a CL as for clauses.